# Block all direct HTTP access to stored CVs (defense in depth; parent /uploads/ also denies).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
